#include "file.h"

//a const variable gets value from the function fcn(). This variable is defined here but will be used in other files like file2.cpp
//extern const int buffsize = 1024;

 const int fcnInFile2()
{
    return buffsize;
}